/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://mrsandco.in', // replace with your domain
  generateRobotsTxt: true, // also generate robots.txt
  sitemapSize: 5000,
};
